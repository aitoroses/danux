// Generated by CoffeeScript 1.6.1

describe('History', function() {
  var history, wardrobe;
  history = null;
  wardrobe = {
    data: 'data',
    modules: 'modules'
  };
  beforeEach(function() {
    return history = new History();
  });
  it('should save the wardrobe', function() {
    history.save(wardrobe);
    return expect(history.stack[0]).toEqual(wardrobe);
  });
  it('should restore the wardrobe', function() {
    var popped;
    history.save(wardrobe);
    popped = history.undo();
    return expect(popped).toEqual(wardrobe);
  });
  it('when save should increases stack size', function() {
    history.save(wardrobe);
    return expect(history.get_size()).toBe(1);
  });
  it('when restore should decreases the stack size', function() {
    history.save(wardrobe);
    return expect(history.get_size()).toBe(1);
  });
  return it('get_stack() should return the history', function() {
    var popped;
    history.save(wardrobe);
    popped = history.undo();
    return expect(history.get_size()).toBe(0);
  });
});
