// Generated by CoffeeScript 1.6.1
var History;

History = (function() {

  function History(stack) {
    this.stack = stack != null ? stack : [];
  }

  History.prototype.save = function(wardrobe) {
    var replicate;
    replicate = $.extend(true, {}, wardrobe);
    this.stack.push(replicate);
    return this.stack;
  };

  History.prototype.undo = function(wardrobe) {
    if (this.stack.length > 0) {
      wardrobe = this.stack.pop();
      return wardrobe;
    }
  };

  History.prototype.get_size = function() {
    return this.stack.length;
  };

  return History;

})();
